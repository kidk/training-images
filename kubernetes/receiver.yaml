apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: receiver
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: receiver
    spec:
      containers:
      - name: receiver
        image: kidk/training-images-receiver
        ports:
        - containerPort: 80
        env:
        - name: RABBITMQ_HOST
          value: rabbitmq
        - name: REDIS_HOST
          value: redis
